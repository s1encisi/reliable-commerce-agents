# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
.venv/
.uv/
uv.lock
# ...but the main Python app's lockfile IS committed so CI installs are
# reproducible and setup-uv's cache key (**/uv.lock) resolves. Same reasoning
# for the tutorials workspace (tutorials/pyproject.toml).
!agents/python/uv.lock
!tutorials/uv.lock
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# .NET / C#
bin/
obj/
*.user
*.suo
*.userprefs
.vs/
.dotnet/
project.lock.json
agents/dotnet/packages/
# Python MCP packages under agents/python/packages/ are intentionally tracked
!agents/python/packages/
TestResults/
[Dd]ebug/
[Rr]elease/
x64/
x86/
*.pdb
*.dll
*.exe
*.nupkg
*.snupkg

# Node
node_modules/
.next/
# Alternate build dirs used by the dual-backend parity run: NEXT_PUBLIC_* is
# inlined at build time, so a frontend pointed at .NET needs its own (see
# web/next.config.ts's NEXT_DIST_DIR note).
.next-*/
out/
articles/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Docker
docker/postgres/data/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Tutorial local-run artifacts
# (workflow.mmd / workflow.dot / session.json are intentionally committed in
# some chapters as reference output — don't ignore those. Only ignore clearly
# transient runtime state: checkpoints written during a run.)
tutorials/*/python/checkpoints/
tutorials/*/python/.checkpoints/
tutorials/*/dotnet/checkpoints/
tutorials/*/dotnet/.checkpoints/

# Portfolio screenshot artifacts (local-only, regenerable)
portfolio-screenshots/
web/e2e/portfolio-screenshots.spec.ts
web/e2e/portfolio-chat-flows.spec.ts
web/e2e/portfolio-code-snippets.spec.ts
web/e2e/screenshots/
.claude/scheduled_tasks.lock

# Generated by scripts/build_docs_site.py — the published docs site.
_site_src/
_site/

# Private repository upload policy: keep local data and personal artifacts local.
# Reviewed synthetic JSON fixtures, source code, and schema SQL remain versioned.
/.claude/
/.codex/
/.agents/
/.local/
/data/
/datasets/
/exports/
/reports/
/results/
/private/
/confidential/
/secrets/
/local-only/
**/credentials/
**/confidential/
**/private-data/

# Environment files: only reviewed templates may be tracked.
.env*
!.env.example
!.env.minimal
*.env
!*.env.example

# Spreadsheets and data extracts (including upper-case extensions).
*.[xX][lL][sS]
*.[xX][lL][sS][xXmMbB]
*.[cC][sS][vV]
*.[tT][sS][vV]
*.ods
*.parquet
*.feather
*.arrow
*.pkl
*.pickle
*.npy
*.npz
*.h5
*.hdf5
*.joblib
*.pt
*.pth
*.onnx
*.rds
*.RData
*.sqlite
*.sqlite3
*.db
*.dump
*.sql.gz
*.sql.zip
*.bak
*.backup

# Keys, certificates, password stores, and local authentication state.
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.kdbx
id_rsa*
id_ed25519*
credentials.json
secrets.json
service-account*.json
application_default_credentials.json
.storage-state*.json
storage-state*.json
auth-state*.json
cookies.txt
.npmrc
# Reviewed pnpm flags only; never add authentication tokens here.
!/web/.npmrc
.pypirc
.netrc

# Local research/business documents and archives require explicit review.
*.doc
*.docx
*.ppt
*.pptx
*.pdf
*.zip
*.7z
*.rar
*.tar
*.tar.gz
*.tgz
*.bundle
