name: text-pipeline
description: 规范的三阶段流水线——转大写、校验、记录日志。
start: uppercase

executors:
  - id: uppercase
    op: upper
  - id: validate
    op: non_empty
  - id: log
    op: prefix
    prefix: "LOGGED: "

edges:
  - from: uppercase
    to: validate
  - from: validate
    to: log
