import { Landing } from "@/components/landing/landing";

/**
 * Project landing — the open-source showcase front door, shown to everyone.
 * "Try the demo" → /shop (public storefront). Account access via Sign in.
 */
export default function RootPage() {
  return <Landing />;
}
